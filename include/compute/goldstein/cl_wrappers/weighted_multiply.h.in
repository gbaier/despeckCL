#ifndef WEIGHTED_MULTIPLY_H
#define WEIGHTED_MULTIPLY_H

#include "clcfg.h"

#include <string>

namespace goldstein
    {
    class weighted_multiply : public kernel_env<weighted_multiply>
    {
        public:
            using kernel_env::kernel_env;

            static constexpr const char* kernel_source =
    "@CMAKE_KERNEL_SOURCE@"
    ;

            static constexpr const char* routine_name {"weighted_multiply"};

            void run(cl::CommandQueue cmd_queue,
                     cl::Buffer interf_real,
                     cl::Buffer interf_imag,
                     const int height,
                     const int width,
                     const float alpha);
    };
}
#endif
